namespace Axe
{		
	typedef map<size_t, Proxy *> TMapRouters;

	class RouterLobby
	{
		size_t getRouter( std::string _login, std::string _password ) throws AdapterNotFoundException;
		
		TMapRouters m_routers;
	};
}