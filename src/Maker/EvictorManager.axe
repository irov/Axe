namespace Axe
{
	exception EvictingNotFoundException
	{
		size_t servantId;
	};

	class EvictorManager
	{
		void set( size_t _servantId, size_t _typeId, Archive _ar );
		Archive get( size_t _servantId, out size_t _typeId ) throws EvictingNotFoundException;
	};
}